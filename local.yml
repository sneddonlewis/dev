- hosts: localhost
  become: true
  pre_tasks:
  - name: Update cache
    apt:
      update_cache: true
    tags:
    - node
    - zsh
  tasks:
  - include_task: ansible-tasks/zsh.yml
  - include_task: ansible-tasks/node.yml
  - include_task: ansible-tasks/nvim.yml
